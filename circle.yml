---

machine:
  services:
    - docker
  environment:
    - PORT=3000
    
dependencies:
  pre:
    - pip install -r requirements.txt
    - pip install -r test_requirements.txt
    - docker build -t undeadops/giles:test .
